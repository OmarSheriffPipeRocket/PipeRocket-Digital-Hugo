{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
{{ $buildTime := now }}
{{ range .Site.Pages }}
  {{- if and (not .Params.private) (ne .Kind "404") (ne .Kind "taxonomy") (ne .Kind "term") -}}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{/* Lastmod priority: frontmatter Lastmod → frontmatter Date → build time fallback */}}
    {{- $lastmod := .Lastmod -}}
    {{- if $lastmod.IsZero }}{{ $lastmod = .Date }}{{ end -}}
    {{- if $lastmod.IsZero }}{{ $lastmod = $buildTime }}{{ end }}
    <lastmod>{{ $lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>
    {{- with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>
    {{- end }}
  </url>
  {{- end }}
{{ end }}
</urlset>
